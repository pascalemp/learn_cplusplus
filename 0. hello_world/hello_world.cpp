#include <iostream>

int main(){

    std::cout << "Hello World!";
    
    // std::getchar() allows keypress to allow a single char
    // to be read from std#in; i.e. allows terminal
    // to not exit prematurely.
    std::getchar();
    
    return 0;

}